<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.WordDictionary" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WordDictionary" parent="Base.Theme.WordDictionary" />


    <style name="CustomIconButtonStyle" parent="Widget.Material3.Button.IconButton">
        <item name="android:padding">4dp</item>
        <item name="android:minHeight">0dp</item>
        <item name="iconSize">24dp</item>
        <item name="iconTint">@null</item>
    </style>

    <style name="CustomIconFilledButtonStyle" parent="Widget.Material3.Button.IconButton">
        <item name="android:minHeight">0dp</item>
        <item name="iconSize">24dp</item>
        <item name="iconTint">?attr/colorPrimary</item>
        <item name="backgroundTint">?attr/colorSurfaceContainerLow</item>
    </style>

</resources>